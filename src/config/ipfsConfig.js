// Defining hostUrl, port and protocol
const hostUrl = "127.0.0.1";
const port = 5001;
const protocol = "http";

// Exporting the hostUrl, port and protocol to use in the other parts of the application
module.exports = {hostUrl, port, protocol};